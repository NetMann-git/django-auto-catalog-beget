```markdown
# Заметки для будущих доработок

- **Кэш страницы товара** – когда будем готовы, адаптировать `@cache_page` для работы с сессией (чтобы не кэшировать блок «Недавно просмотренные»).
- **Улучшение отзывов** – голосование, сортировка, фильтрация, гистограмма, жалобы, видео, расширенные критерии (оценка консультанта и т.д.).
- **Поиск** – при переходе на PostgreSQL заменить Python-фильтрацию на `icontains` или `SearchVector` в `SearchService`.
- **Обновление `requirements.txt`** – обязательно делать после установки каждого пакета.
```